# Script for running automated tests on OSB using Travis-CI, see https://github.com/OpenSourceBrain/osb-model-validation

# This test will validate (most) of the NeuroML 2 files in the current directory using: jnml -validate xyz
target: "*.cell.nml k*.*nml Leak.channel.nml CaPool.nml Figure2A.net.nml"
engine: jNeuroML_validate
